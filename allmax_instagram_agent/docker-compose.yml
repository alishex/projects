services:
  app:
    build: .
    container_name: instagram_bitrix_dm_lead_bot
    env_file:
      - .env
    ports:
      - "8000:8000"
    volumes:
      - ./data:/app/data
      - ./logs:/app/logs
    restart: unless-stopped
